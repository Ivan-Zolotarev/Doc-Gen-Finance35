# Doc-Gen-Finance35 - Система автоматической генерации документации

## Описание проекта

Система для автоматической генерации различных типов документов (финансовые отчеты, договоры, справки и т.д.) на основе шаблонов и данных.

## Возможности

- 🌐 **Веб-интерфейс** - удобная работа через браузер
- 📝 Генерация документов в форматах Word (.docx), PDF, Excel (.xlsx)
- 📄 Использование шаблонов с переменными
- 🔄 Поддержка различных типов документов
- 📊 Автоматическое заполнение данных из форм
- ⚙️ Гибкая настройка через конфигурационные файлы
- 📤 Загрузка собственных шаблонов

## Установка

```bash
pip install -r requirements.txt
```

## Быстрый старт

### Веб-приложение (рекомендуется)

1. Создайте шаблоны (если еще не созданы):
```bash
python templates/create_templates.py
```

2. Запустите веб-сервер:
```bash
python app.py
```

3. Откройте в браузере: http://127.0.0.1:5000

### Использование через Python API

```python
from doc_generator import DocumentGenerator

generator = DocumentGenerator()
data = {
    "company_name": "ООО Система Связи",
    "date": "2024-12-20",
    "amount": "100000"
}

# Генерация Word документа
generator.generate_word("templates/contract_template.docx", data, "output/contract.docx")

# Генерация PDF документа
generator.generate_pdf("templates/report_template.html", data, "output/report.pdf")
```

## Структура проекта

```
Doc-Gen-Finance35/
├── app.py                  # Flask веб-приложение
├── doc_generator/          # Основной модуль генерации
├── templates/              # HTML шаблоны для веб-интерфейса
│   ├── base.html
│   ├── index.html
│   └── templates.html
├── static/                 # Статические файлы (CSS, JS)
│   ├── css/
│   └── js/
├── templates/              # Шаблоны документов
├── data/                   # Исходные данные
├── output/                 # Сгенерированные документы
├── uploads/                # Загруженные файлы
├── config/                 # Конфигурационные файлы
└── examples/               # Примеры использования
```

## Веб-интерфейс

Веб-приложение предоставляет удобный интерфейс для:
- Выбора типа документа (Word, PDF, Excel)
- Заполнения данных через формы
- Загрузки собственных шаблонов
- Скачивания сгенерированных документов

### Основные страницы:
- `/` - Главная страница с выбором типа документа
- `/templates` - Список доступных шаблонов
- `/generate` - API endpoint для генерации документов

## Автор

Разработано в рамках производственной практики

